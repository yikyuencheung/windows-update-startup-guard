interface:
  display_name: "Disable Windows Automatic Updates"
  short_description: "Disable Windows 11 automatic updates at boot"
  default_prompt: "Use $windows-update-startup-guard to install or audit the boot-only Windows Update guard safely."
